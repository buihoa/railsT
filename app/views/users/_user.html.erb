<li>
  <%= gravatar_for user, size: Settings.user.gravatar_small %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
  | <%= link_to t("user.delete"), user, method: :delete, 
  data: { confirm: t("texts.you_sure") } %>
  <% end %>
</li>
